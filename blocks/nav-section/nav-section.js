/*
 * Nav Section block
 *
 * This block is a content container — it holds the sub-links and popular
 * links for one top-level mega-menu section. All visual decoration is owned
 * by `blocks/header/header.js`, which reads the block's rows and rebuilds
 * them into the header's nav. This file exists so that:
 *   1. EDS's lazy block loader resolves successfully (no 404).
 *   2. Authors can author the block in Universal Editor.
 */

export default function decorate() {
  // Intentional no-op: header.js performs all decoration.
}
