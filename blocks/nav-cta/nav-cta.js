/*
 * Nav CTA block
 *
 * The "Always Be Naturing" CTA badge for the header. The header block reads
 * this block's link + image and renders it; this file is a placeholder so
 * the EDS lazy block loader resolves successfully and the block remains
 * authorable in Universal Editor.
 */

export default function decorate() {
  // Intentional no-op: header.js performs all decoration.
}
